import 'package:flutter/cupertino.dart';

bool isDarkMode = false;
ValueNotifier<bool> isDarkModeNotifier = ValueNotifier(false);
var systemBrightness;

// ValueNotifier<bool> isChekedList = ValueNotifier([]);
